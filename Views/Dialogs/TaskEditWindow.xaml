<Window x:Class="TaskForge.Views.Dialogs.TaskEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Редактирование задачи" Height="450" Width="300"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Style="{StaticResource MaterialDesignWindow}">
    <StackPanel Margin="16">
        <TextBlock Text="{Binding IsNew, Converter={StaticResource BoolToNewEditText}}" 
                   FontSize="20" Margin="0,0,0,16"/>
        
        <TextBox materialDesign:HintAssist.Hint="Название" 
                 Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                 Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
        
        <TextBox materialDesign:HintAssist.Hint="Описание" 
                 Text="{Binding Info}" 
                 MinHeight="80" TextWrapping="Wrap" Margin="0,8,0,0"
                 Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
        
        <ComboBox ItemsSource="{Binding Priorities}" 
                  SelectedItem="{Binding Priority}"
                  Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                  materialDesign:HintAssist.Hint="Приоритет" 
                  Margin="0,8,0,0"/>
        
        <TextBox materialDesign:HintAssist.Hint="Награда (опыт)" 
                 Text="{Binding RewardExp}"
                 Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                 Margin="0,8,0,0"/>
        
        <ComboBox ItemsSource="{Binding ProjectsList}" 
                  DisplayMemberPath="Name"
                  SelectedValuePath="Id"
                  SelectedValue="{Binding ProjectId}"
                  materialDesign:HintAssist.Hint="Проект (необязательно)" 
                  Margin="0,8,0,0"/>
        
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Отмена" Command="{Binding CancelCommand}" 
                    Style="{StaticResource MaterialDesignFlatButton}"/>
            <Button Content="Сохранить" Command="{Binding SaveCommand}" 
                    Style="{StaticResource MaterialDesignRaisedButton}" Margin="8,0,0,0"/>
        </StackPanel>
    </StackPanel>
</Window>