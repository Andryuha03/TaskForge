<Window x:Class="TaskForge.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskForge"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Style="{StaticResource MaterialDesignWindow}"
        Title="Task Forge" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">
    <materialDesign:DialogHost x:Name="RootDialogHost" Identifier="RootDialog">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Background="Black">
                <Button x:Name="ProfileBtn" Content="Пользователь" Margin="5 5 5 5" Click="ProfileBtn_Click"/>
                <Button x:Name="TaskBtn" Content="Задачи" Margin="5 5 5 5" Click="TaskBtn_Click"/>
                <Button x:Name="ProjectBtn" Content="Проекты" Margin="5 5 5 5" Click="ProjectBtn_Click"/>
            </StackPanel>
            <Frame Grid.Column="1" x:Name="MainFrame" NavigationUIVisibility="Hidden" />
        </Grid>
    </materialDesign:DialogHost>
</Window>
